import os

# Telegram Bot API Token
# Получите у @BotFather
BOT_TOKEN = "ВАШ_ТОКЕН_БОТА"

# Список Telegram ID администраторов бота
ADMIN_IDS = [
    12345678,  # Замените на ваш реальный ID
]



################# Ниже необязательные настройки ################# 

# Ссылка на GitHub репозиторий для автообновления
# Формат: https://github.com/username/repo.git или git@github.com:username/repo.git
GITHUB_REPO_URL = "https://github.com/plushkinv/YadrenoVPN.git"  # Укажите URL вашего репозитория

# Client Configuration Defaults
DEFAULT_LIMIT_IP = 2  # Ограничение кол-ва одновременных подключений (1 ключ = 2 устройства)
DEFAULT_TOTAL_GB = 1024 * 1024 * 1024 * 1024  # 1 TB в байтах (лимит трафика на ключ)
TRAFFIC_THRESHOLD_FOR_KEY_CHANGE = 20  # Макс. % использованного трафика для смены ключа (20%)

# Rate Limiting Configuration
RATE_LIMITS = {
    "commands_per_minute": 30,              # Максимум команд для обычных пользователей
    "critical_operations_per_minute": 5,    # Лимит для критичных операций (платежи, создание ключей)
}

# Retry Configuration for API calls
RETRY_CONFIG = {
    "max_attempts": 3,      # Максимальное количество попыток
    "delays": [1, 3, 9],    # Задержки между попытками в секундах (экспоненциальная)
}

# Split-config server (умная ссылка для исключений)
# Если используете URL-импорт в клиентах, настройте публичный адрес в БД:
# setting split_config_public_base_url = https://your-domain.tld
SPLIT_CONFIG_ENABLED = False
SPLIT_CONFIG_BIND_HOST = "0.0.0.0"
SPLIT_CONFIG_BIND_PORT = 8081
SPLIT_CONFIG_PUBLIC_BASE_URL = ""

# Optional Reality fallback for split-config generation.
# Use only if 3x-ui API does not return these values in streamSettings.
SPLIT_CONFIG_REALITY_PUBLIC_KEY = ""
SPLIT_CONFIG_REALITY_SERVER_NAME = ""
SPLIT_CONFIG_REALITY_FINGERPRINT = "chrome"
SPLIT_CONFIG_REALITY_SHORT_ID = ""
SPLIT_CONFIG_REALITY_SPIDER_X = "/"
